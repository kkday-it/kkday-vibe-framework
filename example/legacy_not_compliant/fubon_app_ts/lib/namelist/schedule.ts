/**
 * 每天幾點做什麼——**單一來源**。
 *
 * 為什麼要抽出來（Ina 2026-08-17）：「名單 12:00 寄出」這句話散在體檢訊息、催件訊息、
 * 車公司信、工作流程註解裡至少 8 個地方。改時間的時候只要漏掉一處，
 * 機器人就會親口跟 AM 講一個錯的時間——而 AM 是照那句話安排自己的工作的。
 *
 * 這次改動的緣由：**收單 10:59 截止 → DAP 刷新 11AM–12PM → 名單 12:00 寄出**。
 * 刷新與寄名單擠在同一個時刻，靠 wait-dap 排順序，不靠把時間挪開。
 */

/**
 * 名單寄給導遊／車公司的時間（第 1 輪）。
 *
 * **12:00**（Ina 2026-08-17 決定保留這個時間，不因為刷新而往後挪）。
 * 順序是收單 10:59 → DAP 刷新 11AM–12PM → 這裡 12:00。
 * 刷新是「11 點到 12 點之間某個時刻」不是準點，所以 12:00 整不保證刷完 →
 * noon 批的第一步是 `scripts/wait-dap.ts`，**等它刷好再往下走**（最多等 25 分鐘）。
 * 正常情況一次就通過、幾秒結束；只有偶爾晚幾分鐘才真的等，不會整批放棄。
 */
export const NAMELIST_SEND_TIME = "12:00";

/** 第 2 輪：只補寄「第 1 輪沒有司機資訊、現在有了」的那些。 */
export const NAMELIST_CHASE_TIME = "16:00";

/** 司導資訊要給到客人的時間（目前人工，尚未自動化）。 */
export const CUSTOMER_INFO_TIME = "17:00";
