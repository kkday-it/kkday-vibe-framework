# ─── 只填在你自己 Mac 上；這個 .env 別提交、別外傳（已被 .gitignore 擋） ───
# 從 Supabase Dashboard → Project Settings → Database → Connection parameters
# （用「分開的欄位」那組，不是 URI）；用 Session pooler 或 Direct（Port 5432），
# 不要 Transaction pooler（6543）。
PGHOST=
PGPORT=5432
PGDATABASE=postgres
PGUSER=
# 真實密碼，原樣貼一行——不用引號、不用 URL encode（含 @ : / ? # & 都沒關係）；首尾不要有空白。
# 還留著 [YOUR-PASSWORD] / YOUR-PASSWORD 會被腳本擋下。
DB_PASSWORD=

# （選）產物存哪；預設 ~/kkday-go-migration-local（自動 gitignore、chmod 700）。
# ⚠️ 不要設在 iCloud/Desktop/Documents/雲端同步夾——含個資會被同步上雲，腳本會擋。
# OUT_DIR=
