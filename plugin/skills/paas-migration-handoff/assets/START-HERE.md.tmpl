# 給 {{PM 名}} — 從這裡開始（{{系統名}}）

## 一句話：你這次的任務

**盡快把 {{PaaS}} 現在做了哪些事「匯出」（G0）。** 這是最高優先——**整個重構都卡在它**：RD 要先看懂 {{PaaS}} 裡的邏輯，才能照公司規範把系統重建成不依賴 {{PaaS}} 的版本。

> 流程：**① 你匯出（G0）→ ② RD/你的 AI 照規範重構 → ③ 本機 Docker 跑起來 → ④ 灌測試資料 → ⑤ 你 UAT 驗收**。你負責 ① 和 ⑤。**現行線上系統全程不動。**

## 誰做什麼

| | 需要**人（你）**做 | **AI 讀檔就能做** |
|---|---|---|
| 1 | 提供 {{PaaS}} 存取（連線字串 + Dashboard 登入 + token）——只在你機器上給你的 Claude 用 | 照 `G0-EXPORT-GUIDE.md` 匯出（你電腦上的 Claude 跑、留 log） |
| 2 | Dashboard 才能拿的：{{背景服務原始碼、Auth 設定…}}——需要人點畫面/截圖 | ② 照規範重構、③ 本機 compose 跑起來 |
| 3 | ⑤ UAT 驗收（在本機測試版驗，安全） | ④ 灌測試資料、產交付物 |

**你真正要動的就左欄。** 最急＝**G0 匯出**（AI 幫你跑，你給存取）。

## 你的主線：G0 — 最急

👉 打開 `G0-EXPORT-GUIDE.md`，交給你電腦上的 Claude，貼：
> 「照 `G0-EXPORT-GUIDE.md` 幫我做 G0 盤點，全程留 log；跑完先看有沒有查詢失敗再給我結果。要在 Dashboard 點畫面的地方教我怎麼點。**我的連線字串、token 只在我這台機器給你用、不外傳。**」

你只要：
- 備好 {{存取權清單}}——🔒 **只在你機器上給你的 Claude，不用傳給任何人**。（某工具你電腦沒有需 RD 幫忙，走安全管道給——當面/私訊，別貼公開頻道。）
- 匯出完把**結果檔**交 RD（不含你的登入/token）。
- 你的 Claude 最後會產 **`g0-report.html`（完成報告）**——只含狀態摘要、不含個資，**你轉傳給 {{主導者}}**，他就能一眼看到 G0 撈到什麼、還缺什麼。

> ✅ 這案要特別盤乾淨的：{{案件重點，如 N 支背景服務，各自 API 網址/頻率/發送目標——漏一支上雲那條線就死}}。

## 排程、網路這些技術決定，你不用管
已拍板、由 RD 負責。你只要專心把 G0 做完。（細節在 `docs/solution-overview.md`，你不用讀。）

## 資料夾裡有什麼
```
handoff-to-{{pm}}/
├── START-HERE.md          ← 你只需要讀這份
├── G0-EXPORT-GUIDE.md     ← 主要任務：交給你的 Claude 執行
├── CLAUDE.md              給你電腦上 Claude 的規則（你不用讀）
└── docs/                  給你的 AI / RD 看（你不用讀）
```

## 有問題找 {{主導者}}
你的角色：**G0 匯出 + 日後業務決策與 UAT**。實作不用你動手。
